<?php
//	parser.php
//	Script to parse input files and generate the pages.




//$xml_parser = xml_parser_create();

//$depth = 0;
//$tree = array();
//$tree['name'] = "root";
//$stack[] = &$tree;

// Handlers
//xml_set_element_handler($xml_parser, "startElement", "endElement"); 
//xml_set_character_data_handler($xml_parser, "elementContents"); 

//function startElement($parser, $elementName, $attrs) {
	//global $depth;
	//global $stack;
	//global $tree;

	//$element = array();
	//foreach ($attrs as $key => $value) {
		//$element[$key]=$value;
	//}

	//end($stack);
	//$stack[key($stack)][$elementName] = &$element;
	//$stack[$elementName)] = &$element;

	//$depth++;
//}

//function endElement($parser, $elementName) {
	//global $depth;
	//global $stack;

	//array_pop($stack);
	//$depth--;
//}

//function elementContents() {

//}

?>
